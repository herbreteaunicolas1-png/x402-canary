# x402 Factory Procedure — v0.5

`MARKET_CENSUS -> ORGANIC_DEMAND_FILTER -> BUYER_BREADTH -> CONCENTRATION_CHECK -> VALUE_TEST -> REPEAT_LOOP -> CHANNEL_DESIGN -> ECONOMICS_GATE -> LEGAL/TOS -> BUILD -> TEST -> MAINNET -> PAID_CANARY -> MULTI_CHANNEL_DISTRIBUTION -> INDEPENDENT_BUYER_MONITOR -> SCALE/REPRICE/KILL`

Technical gates are automatic and fail-closed. New non-zero spend/pricing requires ECONOMICS_GATE. Provider OAuth/captcha/review is EXTERNAL_AUTH_GATE. No unrelated repository may be used as a runner or fallback deployment surface.
